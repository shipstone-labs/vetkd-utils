{
  "extends": ["//"],
  "tasks": {
    "build": {
      "cache": false,
      "outputs": ["public/build/**"]
    },
    "build:release": {
      "outputs": ["public/build/**"],
      "env": ["NODE_ENV"]
    },
    "dev": {}
  }
}
